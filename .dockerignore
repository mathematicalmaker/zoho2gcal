# Don't copy local env or secrets into the image
.env
.env.*
!.env.example
secrets/*
!secrets/README.md

# Runtime state (created by z2g run; never bake into image)
.z2g-alert-state.json

# Python / build
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/

# Git / docs / assets (optional; include if you want images in image)
.git/
.gitignore
*.md
!README.md
docs/
images/

# Tests (not needed at runtime)
tests/

# Docker (we copy docker/ explicitly in Dockerfile)
# docker/
