# --- Required (fill in during setup) ---
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REFRESH_TOKEN=
ZOHO_CALENDAR_UID=
GOOGLE_CALENDAR_ID=
ZOHO_ACCOUNTS_HOST=https://accounts.zoho.com

# Relative paths to Google OAuth files (in secrets/)
GOOGLE_CLIENT_SECRET_JSON=./secrets/google_client_secret.json
GOOGLE_TOKEN_JSON=./secrets/google_token.json

# --- Defaults / behavior (CLI options can also be set via env for Docker/supercronic) ---
SYNC_LOOKBACK_DAYS=7
SYNC_LOOKAHEAD_DAYS=90
# Optional: explicit range (overrides *_DAYS when set): SYNC_SINCE=-7d SYNC_UNTIL=+90d
# SYNC_SINCE=
# SYNC_UNTIL=
TITLE_MODE=original   # busy | original
Z2G_VERBOSE=0
ICALUID_SUFFIX=zoho-mirror.example.com

# Optional: override Google Calendar reminders (default = use calendar's default)
# Examples: default | popup:10 | popup:10,email:30
# GOOGLE_REMINDERS=popup:10,email:30

# Optional: delete from Google mirrored events in range that are no longer in Zoho (set to 1 to enable)
# Z2G_DELETE_MISSING=1

# Optional: Google auth manual flow (no local server). Z2G_GOOGLE_AUTH_MANUAL=1

# --- Alerting (for "run" only; ignored by "sync") ---
# POST JSON to this URL when consecutive failures >= Z2G_ALERT_MIN_FAILURES (and rate/window allow)
# Z2G_ALERT_WEBHOOK_URL=https://your-ha.example.com/api/webhook/z2g-alert
# Z2G_ALERT_STATE_FILE=.z2g-alert-state.json
# Z2G_ALERT_MIN_FAILURES=2
# Z2G_ALERT_RATE_HOURS=24
# Z2G_ALERT_HOURS_START=8
# Z2G_ALERT_HOURS_END=22
# Z2G_ALERT_TIMEZONE=America/Chicago